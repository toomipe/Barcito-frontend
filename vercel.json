{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/barcito/browser" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/barcito/browser/index.html" }
  ]
}
